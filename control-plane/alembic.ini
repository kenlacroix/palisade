# Alembic config. sqlalchemy.url is intentionally blank — env.py sets it from
# app.config.DATABASE_URL so there is one source of truth for the DB URL.
[alembic]
script_location = migrations
prepend_sys_path = .
sqlalchemy.url =
